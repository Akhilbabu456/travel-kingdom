import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // We can add other standard options if needed, but a clean config is ideal
};

export default nextConfig;
